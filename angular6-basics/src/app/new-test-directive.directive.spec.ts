import { NewTestDirective } from './new-test.directive';

describe('NewTestDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NewTestDirective();
    expect(directive).toBeTruthy();
  });
});
